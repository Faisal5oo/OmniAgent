/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Hide duplicate Next.js dev indicator badges (bottom-left overlay)
  devIndicators: false,
};

module.exports = nextConfig;
